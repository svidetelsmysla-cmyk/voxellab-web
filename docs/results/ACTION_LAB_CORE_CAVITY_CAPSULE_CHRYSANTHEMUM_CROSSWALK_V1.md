# ACTION LAB — CORE / CAVITY / CAPSULE / CHRYSANTHEMUM CROSSWALK V1

Date: 2026-08-17
Status: ANALYTIC PROJECT RECONSTRUCTION / NOT CANON / NOT VALIDATION

## Reason for repair

The high-resolution V14H result `ROBUST_MULTISCALE_CONCENTRATION_NOT_MULTIFRACTAL` says that local strong organization survives grid refinement while the global scalar-compensation radius is domain coupled.

The older two-sided-upor / Chrysanthemum line says that external support can instead be represented by a direction-dependent first-owner terminal front over discrete formed external centres.

These statements are compatible only if several different “boundaries” are not collapsed into one radius.

## Four different objects

### 1. LOCAL MATERIAL ORGANIZATION

```text
R_core / R_relief
```

What it means:

```text
where strong positive-rho / high-order density relief is concentrated.
```

Measured from material state.

### 2. MATERIAL COMPENSATION

```text
M+(R), M-(R), C(R)
R_comp = scale where signed amount becomes strongly compensated.
```

What it means:

```text
amount ledger of the same continuous positive substrate.
```

It may contain:

```text
local cavity / deficit region
+
distributed compensation tail.
```

The global `R_comp` may depend on the containing environment/domain without destroying a local formed structure.

### 3. LOCAL FORCE / SUPPORT CAPSULE

```text
R_cap(Omega)
```

What it means:

```text
local topology of total mechanical response around the formed object:
restoring / saddle / neutral / repelling regions and zero surfaces.
```

It must be computed from the **total physical response** once the material/action/backreaction ledger is available.

Historical W1 equilibrium maps are diagnostics only and cannot yet define the final physical capsule.

### 4. EXTERNAL TERMINAL-OWNER FRONT / CHRYSANTHEMUM

```text
R_term(Omega)
```

What it means:

```text
for each direction, which external formed positive centre is the nearest owner/terminator in the declared first-hit geometry, and at what distance.
```

This is a relational angular geometry. It is not:

```text
material cavity;
local density isosurface;
local force zero;
physical ray impact chronology;
primitive atomism of the field or space.
```

## Corrected two-sided-upor picture

Do not draw:

```text
inner literal wall ---- empty corridor ---- outer literal wall
```

as a necessary ontology.

Use instead:

```text
LOCAL FORMED STATE
  material profile + local response
          |
          | is mechanically conditioned by
          v
EXTERNAL ANGULAR OWNERSHIP / SUPPORT CONFIGURATION
```

The two “sides” of upor are two sides of a relational support problem:

```text
local material/response side
<->
external counter-boundary / ownership side.
```

The remote terminal owner may be far outside the local force capsule. Its contribution can participate in a local restoring topology without the force-zero surface being located at the remote owner's distance.

## New combined hypothesis

A naturally formed clump can in principle possess simultaneously:

```text
an intrinsic local strong-relief scale;
a material compensation tail that extends with the environment;
a local total-force capsule;
a finite external first-owner support depth set by the statistics of other formed centres.
```

Therefore the high-resolution observation

```text
R_comp depends on domain
```

does **not** imply that every physically meaningful boundary of the clump depends on domain.

The decisive test is to measure all four objects separately in one natural solver state.

## Scale-resonance candidate

For an approximately homogeneous family of external formed centres with characteristic size `a`, spacing scale `s`, number density `n_c ~ s^-3`, and geometric angular area `Sigma ~ a^2`, the first-owner terminal depth has the dimensional form

```text
ell_term ~ 1/(n_c Sigma) ~ s^3/a^2.
```

If a hierarchy repeats its dimensionless filling geometry,

```text
a/s = const,
```

then

```text
ell_term/a = const.
```

This supplies a precise candidate meaning of inter-level similarity:

```text
absolute object size changes,
but normalized external support geometry is reproduced.
```

No natural hierarchy selection is established by the current S10 diagnostic because its body radius / tier spacing are prescribed.

## Next source-independent discriminator for R17 output

When R17 produces an unforced natural one-centre state, do not modify its mechanics. Export a read-only packet and compute:

```text
A. R_core / high-q relief scale;
B. local and global material compensation curves;
C. total-force 3D capsule if the total response is available;
D. external terminal-owner map using other naturally formed centres, without an inserted S10 radius;
E. normalized ratios R_cap/R_core and ell_term/R_core;
F. repeat after a naturally occurring split or next-level grouping.
```

The strongest scale-transfer evidence would be:

```text
local material profile collapse
+
local force-capsule collapse
+
external terminal-front collapse
```

after normalizing each level by its own emergent `R_core`, while the microscopic details differ.

## Claim ceiling

This crosswalk does not prove:

```text
that external first-hit ownership is the final force law;
that the current KOU population is physical;
that the S10 support radius is source-derived;
that FCC/HCP is selected;
that a global cavity is unnecessary;
that scale resonance exists in nature.
```

It prevents four different observables from being called one “cavity” or one “upor radius”.